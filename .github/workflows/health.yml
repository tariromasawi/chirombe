name: health
on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * 1"
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsSL https://tariromasawi.github.io/chirombe/app.html >/dev/null
      - run: curl -fsSL https://tariromasawi.github.io/chirombe/data/family.json >/dev/null
