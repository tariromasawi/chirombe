# Security
No secrets in repo. No tokens. Commands are a whitelist via ChirombeBus. Do not eval imported JSON as code. Family names published here are owner-chosen public roster nodes.
Actions: contents write only where a workflow must update derived JSON.
